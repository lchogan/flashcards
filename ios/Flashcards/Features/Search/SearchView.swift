/// SearchView.swift
///
/// Placeholder — replaced in Task 2.19 with a client-side LIKE search across
/// cards and decks. Exists so HomeView compiles while the feature is still
/// being built.
///
/// Dependencies: SwiftUI.

import SwiftUI

struct SearchView: View {
    var body: some View {
        MWScreen {
            MWEmptyState(title: "Search coming in 2.19")
        }
    }
}
